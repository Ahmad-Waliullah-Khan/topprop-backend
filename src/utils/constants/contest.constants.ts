export enum CONTEST_SCORING_OPTIONS {
    STD = 'standard',
    PPR = 'ppr',
    HALF_PPR = 'half-prr',
}

export enum CONTEST_STATUSES {
    OPEN = 'open',
    MATCHED = 'matched',
    UNMATCHED = 'unmatched',
    CLOSED = 'closed',
}
export enum CONTEST_TYPES {
    OVER = 'over',
    UNDER = 'under',
}
